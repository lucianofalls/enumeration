components:
  schemas:
    FinancingOfferStatus:
      type: string
      description: 'Status of the financing offer'
      enum: [ 'Draft'
        , 'Inclusion requested', 'Inclusion approved', 'Inclusion rejected'
        , 'Removal requested', 'Removal approved', 'Removal rejected' ]
    
